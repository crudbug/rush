/*
rush的模板函数十分强大，
不仅支持模板函数作为类的成员，还支持模板动态生成
*/

void main()
{
	int* p=1p
	p.to<char*>.printl
	printl typeof(p.to<char*>)

	A a
	printl a.func<2>
	printl a.func<5>
}

class A
{
	int m_a=2

	int func<T>()
	{
		return m_a+T
	}
}